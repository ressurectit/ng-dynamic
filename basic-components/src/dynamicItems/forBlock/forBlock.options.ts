import {LayoutComponentMetadata} from '@anglr/dynamic/layout';

/**
 * Options for for block component
 */
export interface ForBlockComponentOptions
{
    /**
     * Metadata for template displayed as content of for block
     */
    template: LayoutComponentMetadata|undefined|null;
}

/**
 * Options for for block relations
 */
export interface ForBlockRelationsOptions
{
}