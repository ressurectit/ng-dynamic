import {LayoutComponentMetadata} from '@anglr/dynamic/layout';

/**
 * Options for list block component
 */
export interface ListBlockComponentOptions
{
    /**
     * Metadata for template displayed as content of list block
     */
    template: LayoutComponentMetadata|undefined|null;
}

/**
 * Options for list block relations
 */
export interface ListBlockRelationsOptions
{
}