/**
 * Options for logical and relations
 */
export interface LogicalAndRelationsOptions
{
    /**
     * Array of properties that should be checked for boolean value
     */
    properties: string[];
}