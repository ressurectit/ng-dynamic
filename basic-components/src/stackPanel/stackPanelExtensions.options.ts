/**
 * Options for stack panel flex extension
 */
export interface StackPanelFlexExtensionOptions
{
    //######################### properties #########################

    /**
     * Definition of flex for item in stack panel
     */
    flex: string|undefined|null;
}