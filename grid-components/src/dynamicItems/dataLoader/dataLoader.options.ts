import {LayoutComponentMetadata} from '@anglr/dynamic/layout';

/**
 * Options for data loader component
 */
export interface DataLoaderComponentOptions
{
    //######################### properties #########################

    /**
     * Plugin that represents data loader
     */
    plugin: LayoutComponentMetadata|undefined|null;
}