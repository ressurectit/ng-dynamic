/**
 * Options for dialog metadata selector component
 */
export interface DialogMetadataSelectorComponentOptions
{
    //######################### properties #########################

    /**
     * Name of storage storing current metadata status
     */
    storageName: string|undefined|null;

    /**
     * Indication whether is button for showing metadata selection visible
     */
    showButtonVisible: boolean;
}

/**
 * Options for dialog metadata selector relations
 */
export interface DialogMetadataSelectorRelationsOptions
{
}