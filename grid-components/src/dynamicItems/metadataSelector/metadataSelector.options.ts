import {LayoutComponentMetadata} from '@anglr/dynamic/layout';

/**
 * Options for metadata selector component
 */
export interface MetadataSelectorComponentOptions
{
    //######################### properties #########################

    /**
     * Plugin that represents metadata selector
     */
    plugin: LayoutComponentMetadata|undefined|null;
}