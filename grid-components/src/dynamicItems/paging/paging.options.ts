import {LayoutComponentMetadata} from '@anglr/dynamic/layout';

/**
 * Options for paging component
 */
export interface PagingComponentOptions
{
    //######################### properties #########################

    /**
     * Plugin that represents paging
     */
    plugin: LayoutComponentMetadata|undefined|null;
}