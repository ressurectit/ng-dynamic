/**
 * Options for previous next paging component
 */
export interface PreviousNextPagingComponentOptions
{
    //######################### properties #########################

    /**
     * Initial items per page for paging
     */
    initialItemsPerPage: number|undefined|null;
}
