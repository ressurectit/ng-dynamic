/**
 * Options for data template relations
 */
export interface DataTemplateRelationsOptions
{
    /**
     * Template that can be assigned directly to node
     */
    template: string|undefined|null;
}