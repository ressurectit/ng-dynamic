import {CustomComponentComponentOptions} from '../customComponent';

/**
 * Options for placeholder component
 */
export interface PlaceholderComponentOptions extends CustomComponentComponentOptions
{
}
