import {Provider} from '@angular/core';

/**
 * Enables use of dynamic features in your application setting up all necessary providers
 */
export function provideDynamic(): Provider[]
{
    return [
    ];
}